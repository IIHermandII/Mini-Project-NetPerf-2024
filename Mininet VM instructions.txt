Setup VM by using .ovf file in zip folder
Add host-only network connection in VMbox

Start VM in Mininet_VM
user: mininet
password: mininet

Use cmd or PowerShell to access:
ssh -X mininet@192.168.56.101
password: mininet

cd cs144_bufferbloat/

Run different configurations and do measurements
sudo python bufferbloat.py --bw-host 1000 --bw-net 1.5 --delay 10 --dir ./ --nflows 1 --maxq 100 -n 2

Task 1:
h2 wget http://10.0.0.1 - 0.008s ?? too fast, less than 1 RTT??? SUS

Task 2:
Setup some dataflow to block buffer?
iperf -c 10.0.0.2 -p 5001 -t 3600 -i 1 -w 16m -Z reno > iperf.txt &
h2 tail -f ./iperf-recv.txt
Notice that ping is increasing?
h1 ping -c 100 h2
Download speed suffers?
h2 wget http://10.0.0.1

Bonus commands
sudo mn % test if shit is working
sudo mn -c % in case of crash or fuck up, full rest
